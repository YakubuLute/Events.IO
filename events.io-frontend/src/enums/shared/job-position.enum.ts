export enum JobType {
  Contract = 'contract',
  Freelance = 'freelance',
  FullTime = 'full-time',
  Internship = 'internship',
  PartTime = 'part-time',
  Temporary = 'temporary',
  SelfEmployed = 'self-employed',
}

export enum WorkplaceType {
  Remote = 'remote',
  OnSite = 'on-site',
  Hybrid = 'hybrid',
}

export enum ExperienceLevelType {
  Entry = 'entry',
  Internship = 'internship',
}

export enum LocationType {
  VIRTUAL = 'virtual',
  IN_PERSON = 'in-person',
}

export enum AiGenerateContentType {
  RAW = 'rawtext',
  RICH = 'richtext',
}
