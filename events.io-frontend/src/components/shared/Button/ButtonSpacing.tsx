'use client';

import { styled } from '@mui/material/styles';

const ButtonSpacing = styled('button')({
  padding: 0,
  margin: 0,
  border: 'none',
  cursor: 'pointer',
  background: 'none',
});

export default ButtonSpacing;
